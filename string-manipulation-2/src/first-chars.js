/* exported firstChars */

function firstChars(length, string) {
  return string.slice(0, length);
}

/*
 1.) SLICE off the characters of string, starting at length
 2.) return the result of that expression
 */
