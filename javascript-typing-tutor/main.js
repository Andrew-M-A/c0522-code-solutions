// var $currentCharacter = document.querySelectorAll('span');

function typeCheck(event) {

/*  for (var i = 0; i <= $currentCharacter.length; i++) {
    if ($currentCharacter[i].textContent === ) {
      console.log('correct!');
    }
  }
  */
}

document.addEventListener('keydown', typeCheck);
