/* exported getLastChar */

function getLastChar(string) {
  return string[string.length - 1];
}

/* a function that returns the last character of a string

1.) define a function named getLastChar that takes in one parameter "string", that is any JavaScript String
2.) access the last character of the string by using bracket notation
3.) inside of the bracket, use the length property of the string object and subtract 1, since indexes start at 0
4.) return that entire expression

*/
