/* exported lastChars */
function lastChars(length, string) {
  return string.slice(-length);
}

/*
1.) remove the characters of from the beginning of string... starting at length
2.) return the result of that expression
*/
