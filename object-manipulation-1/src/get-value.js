/* exported getValue */

function getValue(object, key) {
  return object[key];
}

// define a function, "getValue", which passes two parameters, "object", and "key"

// have the function return the value of "object" at "key"
