/* exported getValue */

function getValue(object, key) {
  return object[key];
}

/*
  1.) define a function with two parameters...
    the first, is object, which is any JavaScript object,
    the second is key, which is a Javascript string
  2.) use bracket notation to get the key of object
  3.) return the result of that expression
*/
