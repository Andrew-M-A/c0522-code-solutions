/* exported isUpperCased */

function isUpperCased(word) {
  if (word === word.toUpperCase(word)) {
    return true;
  } return false;
}

/* 1.) use an if statement with the condition,
  if the parameter is strictly equal to the parameter.toUpperCase(parameter),
  return true
  2.) else, return false */
