/* exported setValue */

function setValue(object, key, value) {
  object[key] = value;
  return object;
}

/* 1.) use bracket notation to assign our object[key] a value of value
   2.) return object */
